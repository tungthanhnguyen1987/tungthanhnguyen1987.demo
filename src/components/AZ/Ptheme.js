import { createTheme } from "@mui/material";
export const Ptheme = createTheme({
	palette: {
		primary: {
			main: "#f1ffff",
		},
		secondary: {
			main: "#ff3a",
		},
		/*action: {
			hover: "#f00",
		},
		*/
	},
});
